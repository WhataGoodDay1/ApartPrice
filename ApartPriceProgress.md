# 작업 일지 — 대전 아파트 실거래가 추적

새 세션을 시작하면 이 파일의 최상단(가장 최근 날짜)부터 읽고, "다음에 할 일"을 이어서
진행한다. 하루 작업이 끝나면 맨 위에 그날 날짜로 새 항목을 추가한다(최신이 위로 오게).

---

## 2026-08-31 (4)

### 한 일
- 사용자가 실제 MOLIT_API_KEY를 제공해서 174개 단지 5년치(`--months 60`) 실수집 완료.
- **매칭 품질 정리(여러 라운드)**: 처음엔 name_variants가 나무위키 표기 그대로라
  매칭률이 낮았음(174개 중 매칭 0건 141개). 실제 raw_apt_name을 전수 스캔해서
  국토부 등록명 기준으로 name_variants를 다시 맞추고, 같은 구 안에서 이름이 겹쳐
  다른 단지 데이터가 섞이는 충돌(예: "계룡", "트리풀시티", "베르디움", "현대" 같은
  흔한 브랜드명이 여러 단지에 동시에 매칭되던 문제)을 구조적으로 탐지해서 정리함.
- **구조적 버그 수정**: `match_complex()`가 lawd_cd(구) 구분 없이 174개 전체를
  후보로 봤던 것을 고쳐서, 이제 그 거래가 조회된 구의 단지만 후보로 봄(다른 구의
  같은 이름 단지와 충돌 방지). `collector/molit_api_client.py`.
- 진짜 이름이 겹쳐서 이 API만으로는 절대 구분 불가능한 경우(같은 구에 "계룡", "운암
  네오미아"라는 이름을 쓰는 서로 다른 두 단지가 존재)는 억지로 매칭시키지 않고
  `_ambiguous` 필드로 표시해서 의도적으로 0건 상태로 남김 — 틀린 데이터가 섞이는 것보다
  안전.
- 스캔 중 나무위키 목록에 없던 "강변들금성백조아파트"(서구) 실존을 확인해서 신규
  단지로 추가하고 데이터 분리.
- 최종 상태: **175개 단지, 58,594건**. 매칭 0건 67개(그중 14개는 의도적 보류,
  53개는 원인 불명 — name_variants가 여전히 실제 등록명과 다르거나 5년간 거래가
  없었을 가능성). `scripts/verify_matching.py`로 언제든 재점검 가능.
- 대시보드 재빌드 확인(단지 정보 탭 108개 단지, Raw Data 최근 6개월 5,185건).

### 다음에 할 일 (TODO)
- [ ] `scripts/verify_matching.py` 결과의 "원인 불명 0건 53개"를 시간 날 때 하나씩
      정리(`scripts/raw_apt_names.json`에 실제 등록명 스냅샷 있음 — 대조해서 고치면 됨).
- [ ] `data/collector` 실행할 때마다 매칭 품질이 흔들릴 수 있으니, name_variants를
      바꿀 때는 `scripts/verify_matching.py`로 확인하는 습관 들이기.
- [ ] 둔산자이아이파크, 죽동천년나무아파트는 households 정보를 아직 못 찾음. 필요하면
      확인해서 채울 것.
- [ ] gh CLI 설치 여부는 여전히 미정(`winget install --id GitHub.cli`).

### 참고
- `scripts/raw_apt_names.json`: 서구/유성구 실제 국토부 등록 아파트명 전체 스냅샷
  (2026-08-31 기준, 543개). 새 단지 추가하거나 매칭 문제 생기면 이거부터 대조.
- `scripts/verify_matching.py`: 매칭 0건/이름 충돌 점검 스크립트.

---

## 2026-08-31 (3)

### 한 일
- 단지 정보 탭 신설 관련 브랜치를 main에 병합(`gh` CLI 미설치라 로컬 병합 후 push).
- 단지 상세 차트에 매매/전세 토글 추가, Raw Data/단지 정보 탭 순서를 "단지 정보"가
  먼저 오도록 변경.
- **`config/complexes.json`을 5개 → 174개 단지로 대폭 확장.** 대전 서구 + 유성구의
  500세대 이상 아파트 단지 전체(나무위키 "공동주택/목록/서구(대전)", "목록/유성구"
  기준)를 추가. id 접두사 `seo-001..`, `yuseong-001..`. 기존 5개 중 예미지·칸타빌은
  이 목록과 겹쳐 households(998, 1,132)를 보강했고, 크로바아파트도 이 목록 값과
  기존 설정(1,632세대)이 일치함을 재확인.
- **중요: 이번에 추가한 169개 단지는 MOLIT_API_KEY가 없어 실제 API로 검증 못 함.**
  name_variants는 나무위키 표기(공백만 제거)를 그대로 썼는데, 국토부 등록명은 더
  축약된 경우가 많아서(예: "크로바아파트" 실제 raw_apt_name은 "크로바") 매칭이
  전혀 안 되거나, 반대로 과거 크로바 사례처럼 이름이 겹쳐 엉뚱한 단지 데이터가
  들어올 위험이 둘 다 있음.

### 다음에 할 일 (TODO)
- [ ] **최우선**: `set MOLIT_API_KEY=...` 후 `python collector/molit_api_client.py
      --months 60`(5년치) 정도로 174개 단지 전체 재수집. 국토부 API 특성상 lawd_cd
      (구)당 월별로 그 구의 전체 거래를 가져와 로컬에서 이름 매칭하는 구조라, 단지
      수가 늘어도 API 호출 횟수 자체는 늘지 않음(2구 × 개월 수 × 2엔드포인트).
- [ ] **수집 직후 반드시 점검**: `data/trades.csv`의 `raw_apt_name`을 단지별로 훑어서
      (a) 전혀 매칭 안 된 단지(0건) — name_variants가 너무 길거나 실제 등록명과
      다른 경우, (b) 한 단지에 서로 다른 두 아파트가 섞여 들어온 경우(예: 이전
      크로바 사례) 를 찾아 config/complexes.json을 교정할 것.
- [ ] 둔산자이아이파크, 죽동천년나무아파트는 households 정보를 아직 못 찾음(나무위키
      500세대 이상 목록에 없었음 — 500세대 미만이거나 목록 누락 가능성). 필요하면
      확인해서 채울 것.
- [ ] gh CLI 설치 여부는 여전히 미정(`winget install --id GitHub.cli`) — 계속 로컬
      병합으로 가도 되는지, 나중에 확인.

### 참고
- 174개 단지 목록을 만든 스크립트는 [scripts/generate_complexes.py](../scripts/generate_complexes.py)로
  저장소에 남겨둠(이미 실행 완료라 재실행 불필요 — 참고/재현용).

---

## 2026-08-31 (2)

### 한 일
- `molit_api_client.py` docstring 모순 정리(테스트 완료 상태로), `complexes.json` id 중복
  검증을 collector/dashboard 양쪽 로더에 추가.
- `build_dashboard.py`: `build_rows()`가 요약/표 계산에서 두 번 실행되던 중복 계산 제거.
- 대시보드에 가격대 필터(0~5억/5~10억/10억 이상), 구별 시트탭(대전전체/유성구/서구,
  region에서 자동 추출·엑셀 탭 스타일) 추가.
- 추이(5년) 그래프 이상치 제외 추가 — 처음엔 "최근값 절대기준 7%"로 구현했다가 정상적인
  5년 시세 변동까지 잘려나가는 걸 발견해 사용자와 논의 후 "전월 대비 7% 급변"으로 변경
  (`filter_trend_outliers`, `TREND_OUTLIER_THRESHOLD`). 매매 80%/전세 56% 유지, 월세는
  16%만 유지되는데 이건 필터 문제가 아니라 월세 보증금 자체의 구조적 변동성 때문(정상).
- **main 직접 커밋 관행을 브랜치+PR 워크플로로 전환.** 이번 변경분은 main에 바로 커밋·푸시
  (전환 이전 마지막 예외), 이후부터는 [CLAUDE.md](CLAUDE.md)의 "브랜치 / PR 워크플로" 절차를
  따름(feature 브랜치 → 로컬 테스트 → PR → 머지). `gh` CLI 미설치 상태라 PR은 당분간
  GitHub 웹 compare 링크로 생성.

### 다음에 할 일 (TODO)
- [ ] (선택) `gh` CLI 설치(`winget install --id GitHub.cli`)하면 터미널에서 바로
      PR 생성/머지 가능 — 필요해지면 진행.
- [ ] 매일 새 실거래가 자동 수집(스케줄링) 여부는 여전히 미정 — 필요해지면 논의.
- [ ] 대시보드 추가 검토: 색감/레이아웃, 월세 추이 그래프 처리 방식 등 사용자 확인 후
      추가 조정 여지 있음.

### 참고 (현재 상태 스냅샷)
- `data/trades.csv`: 2,577건, 5개 단지(크로바/둔산자이아이파크/죽동천년나무/칸타빌/예미지),
  최근 수집 시각(`collected_at`) 2026-08-30.
- 대시보드는 매매/전세/월세 통합 표 + 컬럼 필터(아파트명/거래유형/가격대/평형/날짜/전세가율)
  + 구별 시트탭 + 5년 추이(이상치 제외) + 전세가율 구조.
- GitHub 원격 저장소 연결됨: `https://github.com/WhataGoodDay1/ApartPrice`.

---

## 2026-08-31

### 한 일
- 그동안의 작업 내용을 정리해 [CLAUDE.md](CLAUDE.md)(프로젝트 개요)와 이 작업일지를 만듦.
  다음부터는 새 세션에서 이 두 파일만 보면 바로 이어서 작업할 수 있도록 함.

### 다음에 할 일 (TODO)
- [x] `collector/molit_api_client.py` 상단 docstring이 "실제 키로 테스트 안 함"이라
      되어 있는데, 실제로는 이미 데이터가 수집되어 있음(아래 참고). 실제 동작 여부를
      재확인하고 docstring을 현재 상태에 맞게 정리.
- [x] 코드 최적화/리팩터링 대상 파악 및 진행 (사용자가 "아직 완성 아님, 계속 최적화하고
      싶다"고 명시함 — 구체적으로 어느 부분을 다듬고 싶은지 다음 세션에서 먼저 확인).
- [ ] 매일 새 실거래가를 반영하려면 `python collector/molit_api_client.py --months 1` 정도
      주기 실행 필요. 지금은 수동 실행 — 자동화(스케줄링) 여부는 아직 미정, 필요해지면 논의.
- [x] `dashboard/dist.html` 최신 재생성 여부 확인 (`python dashboard/build_dashboard.py`).

### 참고 (현재 상태 스냅샷)
- `data/trades.csv`: 2,577건, 5개 단지(크로바/둔산자이아이파크/죽동천년나무/칸타빌/예미지),
  최근 수집 시각(`collected_at`) 2026-08-30.
- 대시보드는 매매/전세/월세 통합 표 + 컬럼 필터 + 5년 추이 + 전세가율(동일월 매매 대비
  전세 평균) 구조로 되어 있음 (2026-08 개편 버전, `build_dashboard.py` 상단 docstring 참고).

---

## 사용법 메모

- 이 프로젝트 폴더에서 새 Claude Code 세션을 열면 `CLAUDE.md`가 자동으로 로드된다.
  거기서 이 ApartPriceProgress.md를 읽으라고 안내하고 있으니, 그냥 "어제 하던 거 이어서
  하자"라고만 말해도 이 파일의 최신 항목부터 참고해서 이어갈 수 있다.
- 새 항목을 추가할 때는 이 파일 맨 위(첫 `##` 날짜 섹션 바로 아래)에 오늘 날짜로 추가하고,
  "한 일" / "다음에 할 일" / 필요하면 "참고" 세 항목 정도로 짧게 적는다. 너무 장황하게
  쓰지 말고, 다음 세션이 5분 안에 파악할 수 있는 수준으로.
